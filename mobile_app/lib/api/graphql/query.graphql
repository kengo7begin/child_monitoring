query GetRaspiTable($id: String!) {
  getRaspiTable(id: $id) {
    status
    detection
    id
  }
}

subscription OnRaspiTable($id: String!) {
  onRaspiTable(id: $id) {
    id
    detection
    status
  }
}